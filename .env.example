# Copy this file to `.env` and set the path for your machine.
# Path to Ableton Live's Extension Host (or to Live itself).
# Used by @ableton-extensions/cli (npm start) to launch the extension host.
# macOS example:
EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Beta.app
